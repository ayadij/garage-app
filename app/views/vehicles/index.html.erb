index.html
<!-- <p id="notice"><%= notice %></p>

<h1>Vehicles</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Model year</th>
      <th>Make</th>
      <th>Model</th>
      <th>Trim level</th>
      <th>Body style</th>
      <th>Engine type</th>
      <th>Milage</th>
      <th>Vin</th>
      <th>Notes</th>
      <th>Bought date</th>
      <th>Bought milage</th>
      <th>Bought price</th>
      <th>Added</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.name %></td>
        <td><%= vehicle.model_year %></td>
        <td><%= vehicle.make %></td>
        <td><%= vehicle.model %></td>
        <td><%= vehicle.trim_level %></td>
        <td><%= vehicle.body_style %></td>
        <td><%= vehicle.engine_type %></td>
        <td><%= vehicle.milage %></td>
        <td><%= vehicle.vin %></td>
        <td><%= vehicle.notes %></td>
        <td><%= vehicle.bought_date %></td>
        <td><%= vehicle.bought_milage %></td>
        <td><%= vehicle.bought_price %></td>
        <td><%= distance_of_time_in_words(vehicle.created_at, Time.now) %>
ago </td>
        <td><%= link_to 'Show', vehicle %></td>
        <td><%= link_to 'Edit', edit_vehicle_path(vehicle) %></td>
        <td><%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vehicle', new_vehicle_path %>
 -->